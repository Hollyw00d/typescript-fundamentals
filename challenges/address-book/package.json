{
  "name": "address-book",
  "version": "0.0.0",
  "private": true,
  "directories": {
    "lib": "lib",
    "test": "test"
  },
  "files": [
    "lib"
  ],
  "scripts": {
    "clean": "rimraf lib",
    "build": "tsc src/index.ts --outDir lib --types node --module commonjs --target ES2017",
    "test": "mocha --require ts-node/register --require source-map-support/register --timeout 60000 --watch-extensions js,ts,json test/*.test.ts; exit 0"
  },
  "devDependencies": {
    "@types/mocha": "^8.0.3",
    "chai": "^4.2.0",
    "mocha": "^8.1.3"
  }
}
